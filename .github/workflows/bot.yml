name: Masha Bot — BMW M-Power Club — Маша + Редакция

on:
  schedule:
    - cron: '0 */5 * * *'      # Every 5 hours
  workflow_dispatch:                     # Manual trigger

concurrency:
  group: masha-bot-single
  cancel-in-progress: true

permissions:
  contents: write
  actions: write

env:
  BOT_TOKEN: ${{ secrets.BOT_TOKEN }}
  OWNER_ID: ${{ secrets.OWNER_ID }}
  CHANNEL_ID: ${{ secrets.CHANNEL_ID }}
  CHANNEL_USERNAME: ${{ secrets.CHANNEL_USERNAME }}
  POLLINATIONS_API_KEY: ${{ secrets.POLLINATIONS_API_KEY }}
  POLLINATIONS_API_KEY_2: ${{ secrets.POLLINATIONS_API_KEY_2 }}
  CF_ACCOUNT_ID_1: ${{ secrets.CF_ACCOUNT_ID_1 }}
  CF_API_TOKEN_1: ${{ secrets.CF_API_TOKEN_1 }}
  CF_ACCOUNT_ID_2: ${{ secrets.CF_ACCOUNT_ID_2 }}
  CF_API_TOKEN_2: ${{ secrets.CF_API_TOKEN_2 }}
  GH_PAT_TOKEN: ${{ secrets.GH_PAT_TOKEN }}
  GH_REPO: sochiautoparts/masha-bot
  PEXELS_API_KEY: ${{ secrets.PEXELS_API_KEY }}
  PIXABAY_API_KEY: ${{ secrets.PIXABAY_API_KEY }}
  ADMITAD_ADS_FILE: data/admitad_ads.json
  DB_PATH: masha_bot.db
  PYTHONUNBUFFERED: "1"
  LOG_LEVEL: INFO
  NEWS_INTERVAL_MINUTES: "30"
  CHANNEL_POST_INTERVAL_MINUTES: "30"
  PARTNER_POST_INTERVAL_HOURS: "4"

jobs:
  run-bot:
    runs-on: ubuntu-latest
    timeout-minutes: 330

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GH_PAT_TOKEN }}

      - name: Set up Python 3.12
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: 'pip'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Resolve @bmw_mpower_club channel ID
        run: |
          echo "=== Resolving @bmw_mpower_club channel ID ==="
          CHANNEL_INFO=$(curl -s "https://api.telegram.org/bot${BOT_TOKEN}/getChat?chat_id=@bmw_mpower_club")
          echo "Channel info response: ${CHANNEL_INFO}" | head -c 500
          echo ""
          # Extract the channel ID from the response
          CHANNEL_RESOLVED_ID=$(echo "${CHANNEL_INFO}" | python3 -c "import json,sys; data=json.load(sys.stdin); print(data.get('result',{}).get('id','NOT_FOUND'))" 2>/dev/null || echo "PARSE_ERROR")
          echo "RESOLVED_CHANNEL_ID=${CHANNEL_RESOLVED_ID}"
          # Also check current CHANNEL_ID
          echo "CURRENT_CHANNEL_ID=${CHANNEL_ID}"
          echo "=== If RESOLVED != CURRENT, the secret needs updating ==="

      - name: Clean Python cache
        run: |
          find . -type d -name __pycache__ -exec rm -rf {} + 2>/dev/null || true
          find . -type f -name "*.pyc" -delete 2>/dev/null || true
          echo "Python cache cleaned"

      - name: Download Admitad partner data
        run: |
          echo "=== Downloading admitad partner data ==="
          mkdir -p data
          curl -sL "https://raw.githubusercontent.com/creastudioai-beep/pr/main/data/admitad_ads.json" \
            -o data/admitad_ads.json || echo "Failed to download admitad data, will use cached version"
          if [ -f data/admitad_ads.json ]; then
            echo "OK: admitad_ads.json downloaded ($(wc -c < data/admitad_ads.json) bytes)"
          else
            echo "WARNING: admitad_ads.json not found"
          fi

      - name: Restore database from cache
        uses: actions/cache@v4
        with:
          path: masha_bot.db
          key: masha-bot-db-v1
          restore-keys: |
            masha-bot-db-

      - name: Ensure .gitignore allows database
        run: |
          if [ -f .gitignore ]; then
            sed -i '/masha_bot\.db/d' .gitignore
          fi

      - name: Run Masha Bot with auto-restart
        run: |
          echo "=== Starting Masha Bot v2.0 — Маша + Редакция (Image Fix) ==="
          echo "FIX: AIResponse→bytes conversion bug fixed"
          echo "FIX: Proper image extraction from news sources"
          echo "CHANNEL: @bmw_mpower_club — POSTS WITH REAL PHOTOS FROM NEWS"
          echo ""

          MAX_RESTARTS=10
          RESTART_COUNT=0

          while [ $RESTART_COUNT -lt $MAX_RESTARTS ]; do
            echo "=== Bot start attempt $((RESTART_COUNT + 1))/$MAX_RESTARTS ==="

            timeout 18000 python -m bot.main || EXIT_CODE=$?
            EXIT_CODE=${EXIT_CODE:-0}

            echo "Bot exited with code: $EXIT_CODE"

            if [ $EXIT_CODE -eq 0 ] || [ $EXIT_CODE -eq 124 ]; then
              echo "Bot process ended, restarting..."
              RESTART_COUNT=$((RESTART_COUNT + 1))
              sleep 5
            else
              echo "Bot crashed with error code $EXIT_CODE, restarting..."
              RESTART_COUNT=$((RESTART_COUNT + 1))
              sleep 10
            fi
          done

          echo "Max restarts reached, workflow will be re-dispatched."

      - name: Save database
        if: always()
        run: |
          if [ -f masha_bot.db ]; then
            git config user.name "Masha Bot"
            git config user.email "masha-bot@users.noreply.github.com"
            git add -f masha_bot.db || true
            git diff --staged --quiet || git commit -m "db: update $(date +%Y-%m-%d_%H:%M)" || true
            git pull --rebase origin main || true
            git push || true
          fi

      - name: Re-dispatch workflow for 24/7 operation
        if: always()
        run: |
          echo "Re-dispatching workflow for continuous operation..."
          sleep 30
          curl -X POST \
            -H "Authorization: token ${{ secrets.GH_PAT_TOKEN }}" \
            -H "Accept: application/vnd.github.v3+json" \
            "https://api.github.com/repos/sochiautoparts/masha-bot/actions/workflows/bot.yml/dispatches" \
            -d '{"ref":"main"}' || echo "Re-dispatch failed, will retry on next schedule"
